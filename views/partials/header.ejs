<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <meta name="description" content="A Curious Tractor - Dismantling extractive systems through regenerative innovation">
    <meta name="keywords" content="regenerative innovation, community-led change, social impact, First Nations, sustainability">
    
    <!-- Favicon -->
    <link rel="icon" type="image/png" href="<%= typeof basePath !== 'undefined' ? basePath : '' %>/logo/act-logo-horizontal.png">
    
    <!-- CSS -->
    <link rel="stylesheet" href="<%= typeof basePath !== 'undefined' ? basePath : '' %>/css/main.css">
    
    <!-- Additional page-specific styles -->
    <%- typeof additionalStyles !== 'undefined' ? additionalStyles : '' %>
</head>
<body class="page-<%= page %>">
    <!-- Navigation -->
    <nav class="main-nav">
        <div class="nav-container">
            <!-- Left navigation -->
            <div class="nav-left">
                <ul class="nav-links">
                    <li><a href="<%= typeof basePath !== 'undefined' ? basePath : '' %>/" class="<%= page === 'home' ? 'active' : '' %>">Home</a></li>
                    <li><a href="<%= typeof basePath !== 'undefined' ? basePath : '' %>/methodology" class="<%= page === 'methodology' ? 'active' : '' %>">How We Work</a></li>
                    <li><a href="<%= typeof basePath !== 'undefined' ? basePath : '' %>/platforms" class="<%= page === 'platforms' ? 'active' : '' %>">Platforms</a></li>
                </ul>
            </div>
            
            <!-- Centered logo -->
            <a href="<%= typeof basePath !== 'undefined' ? basePath : '' %>/" class="nav-logo">
                <img src="<%= typeof basePath !== 'undefined' ? basePath : '' %>/logo/act-logo-horizontal.png" alt="A Curious Tractor Logo" style="height: 80px; width: auto;" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
                <div style="display: none; align-items: center; gap: 15px;">
                    <svg width="80" height="65" viewBox="0 0 160 100">
                        <g transform="translate(30, 50)">
                            <!-- Tractor body -->
                            <path d="M0,0 L0,20 L10,20 L10,30 L45,30 L45,20 L55,20 L55,-20 L35,-20 L35,-30 L15,-30 L15,0 Z" 
                                  fill="#6B4226"/>
                            <!-- Wheels -->
                            <circle cx="12" cy="40" r="12" fill="#2C3E50"/>
                            <circle cx="12" cy="40" r="8" fill="none" stroke="white" stroke-width="1"/>
                            <circle cx="42" cy="40" r="16" fill="#2C3E50"/>
                            <circle cx="42" cy="40" r="10" fill="none" stroke="white" stroke-width="1"/>
                            <!-- PTO shaft -->
                            <rect x="55" y="-4" width="25" height="6" fill="#FF6B35"/>
                            <circle cx="85" cy="0" r="5" fill="none" stroke="#FF6B35" stroke-width="2"/>
                            <!-- Growth elements -->
                            <circle cx="110" cy="0" r="15" fill="#7CB342" opacity="0.8"/>
                            <circle cx="130" cy="-10" r="12" fill="#7CB342" opacity="0.9"/>
                            <circle cx="145" cy="-5" r="13" fill="#7CB342"/>
                        </g>
                        <text x="15" y="85" font-family="Georgia, serif" font-size="14" fill="#6B4226">a curious tractor</text>
                    </svg>
                </div>
            </a>
            
            <!-- Right navigation -->
            <div class="nav-right">
                <ul class="nav-links">
                    <li><a href="<%= typeof basePath !== 'undefined' ? basePath : '' %>/projects" class="<%= page === 'projects' ? 'active' : '' %>">Projects</a></li>
                    <li><a href="<%= typeof basePath !== 'undefined' ? basePath : '' %>/galleries" class="<%= page === 'galleries' ? 'active' : '' %>">Galleries</a></li>
                    <li><a href="<%= typeof basePath !== 'undefined' ? basePath : '' %>/brand" class="<%= page === 'brand' ? 'active' : '' %>">Brand Guide</a></li>
                    <li><a href="<%= typeof basePath !== 'undefined' ? basePath : '' %>/about" class="<%= page === 'about' ? 'active' : '' %>">About</a></li>
                </ul>
            </div>
            
            <!-- Mobile menu button -->
            <button class="mobile-menu-toggle" aria-label="Toggle navigation">
                <span></span>
                <span></span>
                <span></span>
            </button>
        </div>
    </nav>
    
    <!-- Main content -->
    <main class="main-content"> 